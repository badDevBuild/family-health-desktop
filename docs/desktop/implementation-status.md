# 实施状态

更新时间：2026-09-19

状态说明：`IMPLEMENTED` 表示源码和自动化验证已完成；`VERIFIED` 表示还经过当前目标机的成品实测；`BLOCKED` 表示缺少外部账户、目标机器或签名条件。Mock、真实 Codex、目标平台和公开发行不会混写成同一种“完成”。

| 任务 | 状态 | 当前交付 | 当前证据 / 尚缺条件 |
|---|---|---|---|
| T00 范围与工程边界 | VERIFIED | 独立 `dashboardApp` 工作区、冻结规格索引、ADR、状态/风险/验收记录 | 规格包校验和通过；工程检查通过 |
| T01 Codex App Server | VERIFIED（当前 Mac 文字型流程）/ 图像与中断 PARTIAL | 锁定并随 App 暂存 `codex-cli 0.145.0`；私有 `CODEX_HOME`；严格 stdio 客户端；官方登录；结构化文字/图片 turn；事实阶段禁用 Web Search，派生阶段只开启去标识化的内置 Web Search；中断；未知权限默认拒绝 | 官方账户连接、模型回执、文字型多页 PDF 整篇处理与派生复核已在成品通过；锁定版 Web Search 禁用/实时配置探针通过。图像型真实样例、实际搜索词观测和中断仍需专用样例补验 |
| T02 平台安全与格式 | IMPLEMENTED（Mac arm64）/ DOC 发行门禁 BLOCKED | 文件签名、数量/体积限制；TXT、DOCX、PDF 文本与扫描页、JPEG/PNG/HEIC/HEIF；DOCX 正文块/表格单元格/嵌入图独立定位与视觉输入；旧 DOC 已接入固定哈希/版本的 LibreOffice headless 适配器、独立 profile、超时、PDF 输出校验、转换对象持久化和失败关闭 | arm64 官方包合成 DOC→PDF→证据链真实探针通过，但该上游 App 在本机严格 codesign 校验失败，未进入发行暂存；仍需可分发签名组件及 Intel Mac/Windows 实机 |
| T03 领域契约 | IMPLEMENTED | 严格 Zod 契约；证据、候选、事实/派生、任务、手动资料、后续事项、导出和 IPC 输入均做运行时校验 | TypeScript 严格检查及契约测试通过 |
| T04 存储 | IMPLEMENTED | SQLite WAL、不可变 SHA-256 对象库、schema v28、原件与转换对象关联、升级前一致性恢复点、跨版本整体事务迁移、较新 schema 写入前拒绝、成员 revision、事实/派生分层、审计、幂等、用户事项/补充资料 CAS、报告删除 tombstone；事实冲突同时保留首次提取与独立复核候选、整篇覆盖证明与真实 job/attempt 归属 | 去重、升级备份、多步升级故障回滚、降级拒绝、DB 锁、模拟磁盘满原子回滚、并发冲突、多事项批次恢复、转换视图、失效派生、级联删除、完整性检查均有测试 |
| T05 Electron 骨架 | VERIFIED（Mac arm64） | 单实例、安全 BrowserWindow、sandbox/contextIsolation、最小 preload、sender 校验、CSP、无公共端口、托盘与真实退出；发行冒烟使用仅接受系统临时目录固定前缀的专用参数，同时设置 Electron 与 Chromium 数据路径 | 未签名 unpacked App 实际启动和交互通过；主进程、GPU、网络与 Renderer 的临时目录隔离已由成品进程参数读回 |
| T06 账户与运行时 | VERIFIED（当前 Mac 登录）/ 退出隔离 PARTIAL | 捆绑运行时选择、版本门禁、账户/额度刷新、官方登录、显式退出；退出保留本机档案、撤回 AI 授权并将待发送任务改为等待授权 | 成品 `codex app-server` 子进程和官方账户连接已实测；完整退出隔离与重新登录回执仍需专用场景补验 |
| T07 成员与上下文 | IMPLEMENTED | 离线创建、编辑、归档和恢复成员；归档撤回成员目录/AI 授权但保留历史；显示/临床 revision 分离；本人自述/病史/过敏/用药/自测记录 | 服务层、CAS、归档恢复和授权撤回测试通过；恢复不会静默恢复授权 |
| T08 导入与证据 | IMPLEMENTED（Mac arm64）/ DOC 发行门禁 BLOCKED | 系统多选+桌面拖入、魔数验证、本机不可变保存、重复拦截、受控证据预览；旧 DOC 有组件时生成并标明 PDF 转换视图，无组件/校验失败时保留原件且不进入 AI；资料可移出/重新纳入或硬删除并留 tombstone | 缺失/已安装两条路径、哈希/版本/超时/输出门禁、真实 arm64 合成 DOC 探针和转换视图测试通过；三平台可分发组件仍未放行 |
| T09 收件箱 | IMPLEMENTED | 系统目录选择、稳定等待、递归监控、停用撤权、公共待归属、按状态/成员筛选、批量归属/移出分析、处理全部或只处理选中项；资料建立任务后从收件箱移交到处理中心，失败不重复建批次 | watcher、授权台账、选中资料冻结、任务归属与界面回归测试通过 |
| T10–T14 后台工作流 | VERIFIED（当前 Mac 文字型流程）/ 搜索观测 PARTIAL | 持久调度、DST/睡眠补跑、slot 防重、冻结批次、租约/attempt、提取→独立事实复核→覆盖门禁→事实发布→可选去标识化医学背景搜索→分析/指南→安全复核→派生发布；普通文字型报告在硬输入预算内保持整篇提取与整篇独立复核，模型漏报覆盖清单时会带整篇上下文自动补读一次，单轮最长等待 10 分钟；暂停、停止、重试和恢复 | 经明确授权的多页文字型 PDF 已在成品完成整篇提取、独立事实核对、事实发布、分析与独立安全复核；覆盖补读与多资料阻断恢复有自动测试；本次运行允许 Web Search，但未观察到实际搜索调用，搜索词观测仍需专用虚构样例 |
| T15–T16 桌面看板与任务中心 | VERIFIED（Mac arm64） | 家庭总览、成员六页签、收件箱、处理中心、例外核对、后续事项、设置、证据侧栏、导出；冲突核对显示首次提取与独立复核的差异值，并只要求修正实际冲突字段；收件箱/资料/时间线每批最多渲染 50 项，指标每批 12 组并可继续加载；支持大字号、减少动画和中文/数字日期；无资料/旧分析/待归属等诚实状态 | 1440×900 实际点击；约 200% 缩放仍可操作；55 份收件箱渐进渲染回归通过；差异核对滚动区与双轮值回归通过；显示设置成品可达；入口无假按钮 |
| T17 隐私与安全 | IMPLEMENTED | 原始路径不进 Renderer；授权绑定成员/范围/账户/接收方；Web Search 只用于去标识化的通用医学背景，不改写报告事实；无产品遥测；系统通知可关闭且无健康细节；隐私说明、撤权、受控到期清理和脱敏诊断预览/导出 | IPC、授权、路径、搜索阶段与配置、显示/通知偏好、诊断哨兵和临时目录 canary 测试通过；真实搜索词的独立观测尚未完成 |
| T18 备份与恢复 | IMPLEMENTED | 本机加密备份、固定参数 scrypt、口令校验、独立临时目录验证、可取消恢复、原子切换和启动时中断切换恢复；正式写入前自动创建本机恢复点；schema 升级前另建可识别恢复点；保留 7 个每日 + 4 个每周 | 错误口令、篡改、截断、路径穿越、模拟磁盘写满、用户取消、切换失败、切换窗口进程中断和 schema 迁移失败均不破坏原工作区；恢复与轮转测试通过；自动恢复点暂不提供单点 UI 回滚 |
| T19 质量 | VERIFIED（当前工程） | ESLint、严格类型、206 项常规测试、锁定版 Codex 线程配置探针、生产构建、preload 门禁；固定 40 个纯合成来源包、1,000 个标注字段、8 个留出来源、18 个安全/故障样例、七类真实格式夹具及分项评测器；另覆盖容量基准、Mach-O/PE 识别、发行工件目标架构清理、冒烟工作区路径门禁、大列表渐进渲染、DOCX 表/图、旧 DOC 缺失/转换/哈希/超时门禁、存储故障、schema 升级回滚、恶意备份、恢复取消/故障恢复、格式/调度/流水线和 30 天仿真 | 24 个常规测试文件通过；当前 M2 Max 热缓存快照 P95 559.6ms；40 个二进制夹具在当前 Mac 均通过类型识别；当前 Mac 文字型真实模型链路已通过，独立专业复核、图像型真实样例与三平台评测仍未执行 |
| T20 发行 | VERIFIED（Mac arm64）/ PARTIAL（Mac x64、Windows x64）/ 公开发行 BLOCKED | 品牌图标、三架构独立未签名安装器、锁定版目标 Codex runtime、按 Electron/目标平台预取 SQLite、Mach-O/PE 目标架构硬门禁、第三方许可清单、发行隐私扫描、三架构 GitHub Actions 构建矩阵模板 | arm64 DMG 本机启动通过；x64 DMG 在 Rosetta 下启动，arm64/x64 成品 Electron 都实际加载 SQLite 并完成内存查询；Windows NSIS 中主程序、Canvas、SQLite、Codex 均静态确认为 x86-64 PE；仍缺 Intel Mac/Windows 目标机安装运行、签名、公证、远端 CI 和 libheif 最终合规核验 |

## 当前可交付产物

- Mac arm64 未签名测试 DMG：`release/家庭健康看板-0.1.0-arm64.dmg`
- Mac x64 未签名测试 DMG：`release/家庭健康看板-0.1.0.dmg`
- Windows x64 未签名测试安装器：`release/家庭健康看板 Setup 0.1.0.exe`
- 第三方许可说明：`docs/THIRD_PARTY_NOTICES.md`
- 自动化入口：`pnpm check`
- 本机测试打包入口：`pnpm build:unpack`

## 真实性边界

- 当前 Mac 已在用户明确授权后完成一次真实文字型报告的端到端模型链路；报告内容、身份信息、数据库和模型会话均未进入仓库或发行包。
- “当前 Mac 文字型链路已验收”不代表图像型报告、中断/超时边界或其他目标平台已经通过。
- 该 `.app` 是未签名的本机测试产物，不是可公开分发的安装包。
