# c065b690 评审复核与修复回执（2026-09-22）

跨平台 CI 结果更新于 2026-09-23。

本回执核对 `family-health-review-c065b690` 中的 L01–L07 / T01–T07。评审固定在旧提交 `c065b690`；本轮开始时工作树 HEAD 为 `fb6d621`，因此先按当前源码和实际测试重新判定，不把旧发现原样复制为现状。测试只使用合成资料，没有读取或重处理家庭真实报告。

| 评审任务 | 当前结论与本轮处理 | 验证边界 |
|---|---|---|
| T01 精简主链 | 当前默认 JobRunner 已是未处理块 P01、每成员 P02，P03/P04 按问题触发；旧“主链未接入”的判断已过时。本轮补充块级持久检查点，只有硬限制触发多块时使用；已保存的块在失败后重启不重复发送，成功后清除含原文的检查点。 | 真实 WorkspaceStore + mock runtime：一份、三份，以及 41 片段两块中第二块失败/重试。服务端已接收但本地尚未持久确认的未知回合仍可能重试，不能声称网络层 exactly-once。 |
| T02 全历史与统一输出 | 成员综合读取全部仍纳入且获授权的历史事实，不按最近 N 条截断；发送前重新核对实际待发送观测、来源文件、授权和版本，防止构包后排除旧报告仍发送。成员页面已有 V3 读取；本轮使完整范围的成员摘要导出与 V3 总述/待采纳行动同源，按日期筛选不带出跨期建议。 | A/B/C 完成→关闭/重开 WorkspaceStore→D 只做一次 P01、一次 P02；P02 含 A/B/C/D、旧已完成行动仍保持完成；排除 B 后新请求不含 B，其他成员资料不混入。 |
| T03 提示词与运行时 | D00 与 P01–P04 在当前版本已接入，线程探针检查禁用/实时 Web Search 配置。本轮给 P02/P04 补短规则：newDocumentIds 不是唯一范围；有来源的既往诊断可为 `documented + historical`；`treatment_discussion` 不豁免直接药物指令。成员提示词版本升为 `member-assessment-v8`，旧 V3 综合不被静默复用。 | 当前真实模型合成 A005 仅为 P02 单阶段样例，不能代替完整真实报告流程。内置 Web Search 的真实查询词仍无法发送前本地拦截，用户选择保持开启并提示风险。 |
| T04/T05 局部医学规则与复核闭包 | 修复同句家族史误拒、亲属诊断误认、错标 `interpretation` 的高后果判断、总览/系统无依据的高后果表述，以及 `treatment_discussion` 中直接开始处方药的漏检。提交前独立审查又补上“不能排除／待排”高后果疾病表达的漏检。P04 替换后按新依赖闭包处理；同路由理由但替换出新的高影响内容时隔离。规则版本升为 `assessment-validation-v10` / `clinical-router-v3`。 | 合成 T03–T18 与新增表达回归；局部正则是保守安全门禁，不等于完整医学语义理解或专业诊断验证。 |
| T06 V3 生命周期 | 事实、背景、核对范围改变时同事务将 V3 标 stale；current 读取再校验版本和核对范围。发布边界再次检查完整 schema、节点/证据引用与 held 节点不在正文。永久删除从当前数据库清除该成员旧 V3 正文及由其采纳的 AI 行动，用户自建事项保留；删除确认界面明确告知。分块检查点在成功、取消、删除时清理。 | 真实 Store 合成闭环已测。既有恢复点、外部备份与已导出文件可能仍保留旧资料，删除当前工作区不承诺擦除这些副本。 |
| T07 交付证据 | 下列命令/回执列明实际执行与未执行项，不把纯函数测试、旧版本真实模型回执或之前的 CI 当作本轮完整验收。当前代码提交 `754d92b` 的三平台无签名 CI 已通过。 | 签名/公证包与目标机安装仍为 **NOT_RUN**；真实家庭报告自动重跑与独立医学专家审核均为 **NOT_RUN**。 |

## 可复核的本轮运行结果

- 应用层 mock runtime + 真实 SQLite：单份小报告 `P01×1 + P02×1`；同批三份 `P01×3 + P02×1`；重启后第四份 `P01×1 + P02×1`，历史 A/B/C 未重提取；多块失败重试只请求未完成的第二块。页面读取不会自行发起模型回合。
- 请求契约：D00 为 `health-runtime-v3`；提取模板 P01 / `extract-v5`，成员综合模板 P02 / `member-assessment-v8`；P03、P04 条件触发，采用各自结构化 output schema。事实阶段关闭 Web Search；综合阶段是否允许搜索由运行时授权与设置决定，而不是提示词自行扩大权限。
- P02 的合成历史来源和授权：回归测试读取当批 consent 的 `historicalObservationIds`，与 A/B/C 既有观测 ID 对齐，D 只在本批 documentIds；P02 输入含四期事实与程序趋势，跨成员独有病史未出现。排除 B 后 P02 不含 B 的事实。真实个人来源 ID、正文和令牌均未写入本回执。
- 真实 Codex 合成质量样例（最终 v10 规则前运行）：`gpt-5.6-sol` / `medium`，A005 既往高血压病例 P02 一次，约 69 秒、输入/输出 17,785/1,960 token，schema 有效、当时 v9 本地 issues 0、P04 目标 0、Web Search 0。模型保留“2022 年既往诊断”与“当前血压及控制状态未知”的区别。[脱敏机器回执](review-c065b690-synthetic-A005-receipt.json)随仓库保存；原始回执在隔离临时目录 `fhd-lean-v3-synthetic-eval-C5j1Ed/receipt.json`，日后可能清理。该样例不是医生审核，也不代表全部病例通过。
- 工程验证：定向提取/提示词 52 项通过；成员/路由/导出/存储定向 137 项通过；新增界面确认与 JobRunner 定向 34 项通过。最终最新工作树的完整 `pnpm check` 已通过：lint、Node/Web 类型检查、Codex 线程配置探针、39 个测试文件／410 项测试（含新增的 3＋1 趋势断言、写库门禁和高后果表达测试）、44 份文档链接、生产构建和构建产物校验。
- 跨平台无签名 CI：[第二轮运行 35752849667](https://github.com/badDevBuild/family-health-desktop/actions/runs/35752849667) 对代码提交 `754d92b` 的 macOS arm64、macOS x64、Windows x64 三个 job 均成功；各平台均完成 `pnpm check`、目标平台打包、发布包边界扫描与测试产物上传。第一轮运行 `35751677364` 的 Windows job 曾因 5 秒测试超时及超时后的 SQLite 文件占用失败；修复为 Windows 30 秒集成测试时限，并在临时目录清理前关闭工作区数据库，第二轮已证实通过。

## 未完成项与产品边界

1. 三平台无签名包已在 CI 构建并扫描，但 macOS x64、Windows x64 等目标机的实际安装、交互验收，以及公开发行所需的签名/公证均未完成；CI 成功不等于可公开发行。
2. 模型语义质量不是“本地规则通过”就可认定正确。需要在纯合成金标准和专业复核下持续评估；本轮 A005 只证明一个正例。
3. 成员综合允许的内置 Web Search 无法由应用拦截发送前实际查询词；产品已向用户说明该风险，不能把提示词约束或发送后观察当成技术硬门禁。
4. 已有恢复点/外部备份是独立保留对象；当前数据库永久删除后，不应声称旧备份也被擦除。
